# Deployed Space only. CPU wheels, and nothing that is not needed to serve.
torch
numpy
pillow
# Every photograph an iPhone takes is HEIC, and Pillow cannot open one
# unaided. Without this the Shortcut's first upload is a 415.
pillow-heif
fastapi
uvicorn
python-multipart
# Captioning, for the photo search. Optional at runtime: without
# ANTHROPIC_API_KEY the feature simply reports itself unavailable.
anthropic
# Face detection and recognition, entirely local: YuNet + SFace under OpenCV.
# Headless because a server has no display. Optional at runtime.
opencv-python-headless
